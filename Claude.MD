# CLAUDE.md — Wardro Alpha 0.95
> الدليل التشغيلي لـ Claude Code | اقرأ هذا أولاً قبل أي خطوة

---

## المشروع
تطبيق ذكاء اصطناعي لتنسيق الملابس الرجالية — السوق الجزائري، يبدأ من ورقلة.
- المطور: بوجمعة (Djimi) — ورقلة، الجزائر
- الرابط: https://hawwadmowissa-art.github.io/wardro-alpha-0.95/
- الـ Repo: hawwadmowissa-art/wardro-alpha-0.95

---

## Stack الثابت — لا يتغير أبداً
| التقنية | الحالة |
|---------|--------|
| HTML + CSS + Vanilla JS | ✅ ثابت |
| Anthropic Claude API | ✅ ثابت |
| Supabase (eu-central-1) | ✅ ثابت |
| GitHub Pages | ✅ ثابت |
| React / Next.js / أي Framework | 🚫 ممنوع |
| إعادة بناء من صفر | 🚫 ممنوع |

---

## هيكل الملفات
```
wardro/
├── index.html          ← التطبيق الكامل (HTML + CSS + JS)
├── admin.html          ← لوحة تحكم المشرف — مستقلة تماماً
├── style.css           ← الأنماط المنفصلة
├── app.js              ← المنطق المنفصل
├── supabase-config.js  ← محلي فقط — لا يُرفع أبداً
├── products.json       ← بيانات ديمو
├── CLAUDE.md           ← هذا الملف
├── AGENTS.md           ← أدوار Workers
├── .gitignore          ← يحتوي supabase-config.js
└── docs/
    └── PRD.md          ← وثيقة المتطلبات
```

---

## ما تم إنجازه ✅
| المرحلة | التفاصيل | التاريخ |
|---------|----------|---------|
| Phase 1 — Deploy | GitHub Pages + API Key في localStorage | مايو 2026 |
| Phase 2 — Database | Supabase متصل، sellers + products + RLS | يونيو 2026 |
| Restructure Phase 1 — Editor Mode | عمود `slider_type` (none/hero/main_hero) بدّل `hero` (bool)؛ Editor مقسّم لـ 3 أقسام مستقلة (Main Hero Slider / Hero Slider / Products) كل واحد بـ dashed "+" tile خاص؛ Edit form فيه segmented control لتغيير القسم؛ حذف toggleHero() وQuick Add row | يونيو 2026 |
| Seller Approval — Step 1 (DB) | `sellers.approval_status` (default 'pending') + `products.approval_status` (default 'approved')؛ RLS مُحدَّث: public يرى المعتمدين فقط؛ is_admin() + seller_is_approved() كـ SECURITY DEFINER functions؛ البائعين السبعة الحاليين أُبقوا بحالة 'approved' | يونيو 2026 |
| Seller Approval — Step 2 (UI) | شاشة `#s-pending` جديدة: WR emblem + "حسابك قيد المراجعة" + اسم المتجر + إيميل + زر تسجيل خروج؛ Routing محدَّث في Registration/SignIn/Session Restore — pending/rejected يذهبون لـ #s-pending، approved للـ editor كالمعتاد | يونيو 2026 |
| Seller Approval — Step 2 (Logout Fix) | logOut() يُعيد ضبط حقول reg + signin (قيم + أزرار + أخطاء)؛ goSeller() يوجّه لـ signin بدل reg؛ doSellerReg() يُجرّب sign-in صامت عند "already registered" ويوجّه حسب approval_status | يونيو 2026 |
| Seller Approval — Step 3 (Admin) | `admin.html` مستقل: بوابة تسجيل دخول + فحص ADMIN_EMAIL؛ تابين (بائعون قيد المراجعة / منتجات Main Hero)؛ موافقة/رفض مع تأكيد + toast؛ Session Restore للمشرف؛ محمي من غير المصرح لهم | يونيو 2026 |
| Security Audit — API Key + XSS | حذف `getApiKey/showApiKeyModal/saveApiKey` + cleanup `wardro_claude_key`؛ إضافة `esc()` + `safeUrl()` في `app.js`؛ تأمين كل نقاط `innerHTML`: بطاقات Editor/Show/Browse/Saved/Discover/Results/Hero Sliders + Product Detail + Top Stores (data-attributes بدل onclick injection) | يونيو 2026 |
| Phase 3 DB Foundation — Migration | `products.product_type` (text NOT NULL DEFAULT 'shirt' CHECK IN shirt/pants/shoes/jacket/accessory)؛ `products.color_tags` (text[] NOT NULL DEFAULT '{}')؛ جدول `user_behavior_log` جديد (id/user_id/action/product_id/store_id/product_type/color_tags/product_category/created_at) مع RLS: users يكتبون/يقرأون سجلاتهم فقط، admin يقرأ الكل عبر is_admin()، لا UPDATE/DELETE (immutable log) | يونيو 2026 |
| Phase 2A — Product Type + Dynamic Sizes | Editor: حقل "Product Type" مطلوب (Shirt/Pants/Shoes/Jacket/Accessory) قبل size selector؛ sizes ديناميكية حسب النوع (S-XXL للملابس، 39-46 للأحذية، One Size للاكسسوار)؛ زر الحفظ disabled حتى يُختار النوع + الاسم + السعر؛ Edit mode يعبئ مسبقاً + يفلتر sizes غير المتوافقة؛ product_type يُحفظ في DB | يونيو 2026 |
| Phase 2B — Color Chips + Hide Traditional | Editor: استبدال حقل اللون النصي بـ 12 chip ملونة (black→rust) مع سواتش + تسميات عربية؛ multi-select اختياري؛ color_tags يُحفظ في DB؛ إشعار "اختر من اللوحة الجديدة" للمنتجات القديمة؛ حذف Traditional من cat-btns + إشعار للمنتجات التقليدية يجبر إعادة الاختيار؛ زر الحفظ يتطلب الآن product_type + type + name + price | يونيو 2026 |
| Phase 2C — Save Event Logger | Customer: _logBehavior(action, product) fire-and-forget يُدرج في user_behavior_log عند save/unsave؛ يتخطى صامتاً إذا anonymous أو seller؛ console.error عند الفشل فقط؛ _svItemMap يُحفظ في renderSaved() لتوفير بيانات المنتج لـ removeSavedItem؛ query loadSaved مُوسَّع بـ seller_id/product_type/color_tags/type | يونيو 2026 |
| Phase 3A — Home 11-Section Redesign | Customer Home: 11 قسم ثابت متبادل (عمودي/أفقي)؛ 5 grids عشوائية (Fisher-Yates per session)؛ 4 strips أفقية (Casual/New This Week/Sport/Formal)؛ Top Stores دائم؛ R1: strip تختفي إذا < 2 منتجات؛ R2: Top Stores دائمة إذا 1+ seller؛ 15-20% تكرار مضبوط؛ توزيع 12/8/6/6/4 للـ grids؛ Section 12 مؤجلة لـ Phase 3B | يونيو 2026 |
| Ensemble — Product Type (Batch 1) | نوع سادس "Ensemble" في Editor (بعد Accessory)؛ مقاسات قميص S-XXL؛ حقل "الحالة" (🔓 Open default / 🔒 Close) يظهر للـ ensemble فقط؛ عمود `products.ensemble_state` (nullable، CHECK open/close)؛ CHECK constraint لـ product_type وُسِّع ليشمل ensemble + jeans (كان jeans مفقوداً — أي حفظ Jeans كان يفشل صامتاً!)؛ Discover: خيار 🕴 Ensemble كـ bucket مستقل (لا يتداخل مع Pants)؛ شارة "ENSEMBLE 🔓/🔒" ذهبية صغيرة على بطاقات الـ Catalog | يوليو 2026 |
| Outfits — Batch 2 (Editor Tabs + My Outfits + Build Collection) | Editor مقسّم لتابين: Catalog (المحتوى القديم كما هو) + Outfits؛ sub-tabs pill: My Outfits (default) / Other Sellers' Outfits (placeholder "قريباً")؛ My Outfits: empty state (أيقونة علّاقة SVG ذهبية + زر إنشاء) أو grid 2-col من بطاقات outfit بوضعين — Cover (صورة full-bleed) / Cards (حتى 3 mini-cards صورة+نوع بحد ذهبي 15%)؛ badge Cover/Cards أعلى-يسار؛ اسم Fraunces ذهبي + "N قطعة • X% Match"؛ Build Collection modal كامل الشاشة: اسم + غلاف اختياري (رفع لـ `product-images/outfits/{seller_id}/{outfit_id}.jpg`) + شبكة قطع مرقّمة مع قائمة "..." (إزالة/تقديم/تأخير) + خلية "+" dashed تفتح picker من كتالوج البائع + سعر إجمالي auto-sum + Exclusive + ملاحظة + info bar (عدد/متجر/سعر/حالة) + toggle مفتوح/مقفل بالـ header + Done/إلغاء/حذف؛ Match% ثابت: 2→80، 3→87، 4+→92؛ جدولا `outfits` و`outfit_items` (position، added_by، CASCADE) مع RLS: قراءة عامة، كتابة للمالك فقط (outfit_items عبر EXISTS على الأب) | يوليو 2026 |
| Outfits — Simplification (إزالة المشاركة بين البائعين) | حذف sub-tabs (My Outfits / Other Sellers') من تاب Outfits — الشبكة تظهر مباشرة؛ حذف toggle مفتوح/مقفل من Build Collection modal (`saveOutfit` يكتب `state:'close'` دائماً)؛ حذف حقل "الحالة" 🔓/🔒 من Ensemble في Catalog (`saveProduct` يكتب `ensemble_state:'close'` دائماً عند product_type='ensemble')؛ الأعمدة `outfits.state` و`products.ensemble_state` بقيت في DB (inert، دائماً 'close') — لم تُحذف | يوليو 2026 |
| Collections — Public Outfit Grid + Detail Sheet (Show Mode + Guest Store View) | تاب Collections في #s-show (كلا الوضعين) أصبح فعّال: `renderCollectionsPublic()` يجيب outfits+outfit_items(product) للـ seller_id الحالي (Show Mode: current user، Guest: `_guestSellerId`) عبر hook في `switchShowTab`؛ يعيد استخدام `.of-grid/.of-frame/.of-frame-img` + mini-stack fallback من Editor بدون تعديل؛ بطاقة عامة جديدة بكلاسات `.col-heart` (قلب أعلى-يمين، نمط شبيه بـ pd-heart-btn لأن "br-prod-card heart" غير موجود أصلاً بالكود) و`.col-price` (بدل badge Cover/Cards المحذوف + بدل Match%؛ حصري بالذهبي أو `${total_price} دج`، يختفي السطر إذا فارغ وغير حصري)؛ **جدول DB جديد `saved_outfits`** (customer_id/outfit_id/created_at + RLS مطابقة لـ saved_items) — أُنشئ بعد سؤال المستخدم لأن `saved_items` الحالي عمود `product_id` NOT NULL بـ FK على `products` فقط، لا يقبل معرّف outfit | يوليو 2026 |
| Collections — Outfit Detail Sheet scroll fix + بار أزرار مبسّط | `.od-sheet` (لا `.od-info`) هو حاوي التمرير الوحيد الآن (`overflow-y:auto` انتقل من الداخل للخارج) — كان `.od-info{overflow-y:auto;flex:1;min-height:0}` محشور تحت `.od-frame` بنسبة 3/4 فيسبب نافذة تمرير داخلية صغيرة مقصوصة أفقياً وعمودياً؛ الترتيب الجديد: اسم → سعر → "عن التنسيقة" → عنوان "القطع" + شريط أفقي (`.od-pieces-row` بلا تمرير عمودي)؛ حُذف `.od-desc` (الوصف يظهر مرة واحدة فقط ضمن "عن التنسيقة")؛ زر "حفظ" السفلي حُذف نهائياً — `.od-actions` بقي بزر واتساب فقط (عرض كامل، `position:sticky;bottom:0` ضمن `.od-sheet`)؛ القلب أعلى-يمين الصورة (`odToggleHeart`) أصبح هو آلية الحفظ الفعلية الوحيدة بالـ sheet (يكتب لـ `saved_outfits` مباشرة، بدل كونه تجميلياً فقط كـ pdToggleHeart)؛ `odSaveOutfit()` و`.od-eq-btn` حُذفا (كود ميت) | يوليو 2026 |
| Product Type — إضافة "Sandals" | نوع سابع `sandals` أُضيف لكل قوائم product_type: زر Add/Edit Product (`product-type-btns`)، `_AP_TYPE_SIZES` (مقاسات 39-46 مثل shoes)، فلتر Discover (`dc-type-menu` + `_dcPieceLabels`/`_dcPieceIcons` 👡)، `otherTypes` بكويري "قطع تتناسق معها"، `_OF_TYPE_LABELS`، نص تلميح Build Collection picker؛ **CHECK constraint** `products_product_type_check` وُسِّع ليشمل sandals (لولاها كان أي حفظ Sandals يفشل بصمت كما حصل سابقاً مع jeans) — تم بعد سؤال المستخدم رغم أن المهمة ذكرت "no migration needed" | يوليو 2026 |
| Editor — رفع الصور من 5 إلى 10 | حد صور المنتج الواحد رُفع من 5 إلى 10 في 4 مواضع بـ`app.js` (شرط ظهور زر "+"، نص التلميح والتحذير، فحص `addProductImages`، و`slice(0,10)` النهائي عند الحفظ) + نص التلميح الافتراضي بـ`index.html`؛ لا تغيير بمخطط DB أو bucket (عمود `images` أصلاً `text[]` بلا حد أقصى) | يوليو 2026 |
| PWA — Android Back Button History | `navigateTo()` (الدالة المركزية الوحيدة لكل انتقال شاشة — كل التابات الفرعية Saved/Discover/Profile تمر عبرها أصلاً) تدفع الآن `history.pushState({screen:targetId},'','')` عند كل انتقال حقيقي، مع باراميتر ثالث اختياري `fromHistory` لتفادي push مضاعف عند العودة عبر الخلف؛ `popstate` listener واحد عند تحميل السكريبت يستدعي `navigateTo(screen,'z-axis',true)` (نفس نظام التنقل الموجود، بدون إنشاء نظام جديد)؛ `history.pushState({screen:'s-splash'},'','')` أول سطر بالملف كـ"أرضية" تمنع خروج التطبيق من أول ضغطة رجوع؛ لم تُمس أي أنيميشن أو منطق تنقل موجود | يوليو 2026 |
| PWA — Offline Fallback Page | `offline.html` جديد بالجذر: WR mark نصي ذهبي + "WARDRO" + "لا يوجد اتصال بالإنترنت" + زر "إعادة المحاولة" (`window.location.reload()`)؛ كله inline بلا أي طلب خارجي (لا @import للخطوط — يعتمد fallback نظامي `Georgia,serif`/`Segoe UI,sans-serif` لأن أي طلب شبكة سيفشل أصلاً وهو offline)؛ `service-worker.js`: أُضيف `./offline.html` لمصفوفة `SHELL` (يُخزَّن عند `install`)، وسلسلة fallback الـ navigate صارت `caches.match('./') → './index.html' → './offline.html'` (نفس `CACHE_VERSION='wardro-v1'` — تغيّر محتوى الملف كافٍ ليُعاد تشغيل install/activate عند الزيارة القادمة)؛ منطق cache-first للأصول الثابتة (CSS/JS/صور) لم يُمس | يوليو 2026 |
| Product Detail — اتجاه Swipe للصور | `_pdCarouselTouch` (touchend): قُلب الـ ternary (`dx<0?idx-1:idx+1` بدل `idx+1:idx-1`)؛ التتبع الرياضي للكود الأصلي كان يطابق نظرياً "swipe left→next" مسبقاً، لكن المستخدم أكّد اختباراً فعلياً على جهاز أن السلوك المعروض كان معكوساً — تم الانعكاس بناءً على ذلك التأكيد المباشر؛ `_wireHeroTap` (hero sliders) لم يُمس، غير مرتبط بهذه البطاقة | يوليو 2026 |
| Product Images — إصلاح Silent Failures + ضغط الصور | `saveProduct` (صور المنتج + صور التفاصيل) وحفظ التنسيقة (outfit cover) كانوا يبتلعون أخطاء رفع Supabase Storage بصمت (`if(!upErr){...}` بلا else) — أُضيف `toast` عربي عند فشل الرفع (نفس نمط `previewStoreProfile` الموجود مسبقاً)؛ دالة جديدة `_compressImage(file,maxPx=1200,quality=0.82)` (Canvas API) تُطبَّق على صور المنتج والتفاصيل قبل الرفع — خفّضت الحجم من ~2.5-3.5MB إلى ~150-165KB للصورة الواحدة؛ `pdSelectColor` أيضاً أُصلح (لون بدون صورة مقابلة يذهب لآخر صورة متاحة بدل ألا يفعل شيئاً) | يوليو 2026 |
| تذكرني + Google Sign-In (Customer فقط) | Checkbox "تذكرني" (`.remember-row/.remember-box`) أُضيف تحت حقل كلمة المرور بشاشة Seller Sign-In (`si-remember`) ونافذة Customer Auth (`ca-remember`) — بلا منطق إضافي لأن Supabase يحفظ الجلسة افتراضياً؛ زر "متابعة بحساب Google" (SVG multicolor inline، بدون CDN) أُضيف في Customer Auth Modal فقط (بعد divider "أو")، يستدعي `doCustGoogleAuth()` الجديدة في `app.js` (`signInWithOAuth({provider:'google',redirectTo:'.../wardro-alpha-0.95/'})`)؛ شاشة Seller Sign-In لم تُمس بخلاف الـ checkbox؛ ⚠️ هذه الميزة كانت مدرجة سابقاً ضمن "خارج النطاق الآن" (تسجيل الدخول/Auth) — نُفذت بعد سؤال المستخدم مباشرة وتم حذفها من قائمة الاستثناءات | يوليو 2026 |
| Order Confirmation — Animation Shell (UI فقط) | عند الضغط "تأكيد الطلب" بنموذج الطلب: منطق الإرسال القديم بـ `cfConfirmOrder()` عُلِّق بتعليق `// TODO: order submission` (لم يُحذف) واستُبدل باستدعاء `ocStart()` الجديدة؛ overlay جديد `#oc-overlay` (z-index:950، فوق `cf-overlay` 650 و`cust-auth-modal` 700) بمرحلتين: Phase 1 "جاري إرسال طلبك..." (شعار W بتوهج ذهبي نابض + حلقة دوارة + connector منقّط + 3 خطوات تظهر بفارق 0.7ث عبر `animation-delay` على `#oc-step-1/2/3` — المدة الكلية تتزامن تلقائياً مع مهلة الـ2.5ث بـ`ocStart()`)، ثم Phase 2 "تم استلام طلبك" (علامة صح SVG ترتسم بـ`stroke-dashoffset` + شرارات ✦ + رقم طلب عشوائي `#WR-XXXXX` عبر `Math.random()` + جدول حالة 3 صفوف RTL)؛ `ocCloseToHome()` تغلق كل الطبقات (`oc-overlay` + `cf-overlay` + `closeProdDetail()` الموجودة) وتنتقل لـ `s-browse` عبر `navigateTo` الموجودة؛ CSS keyframes فقط، لا مكتبات خارجية؛ لا لمس لحقول نموذج الطلب أو زر واتساب أو أي شاشة أخرى | يوليو 2026 |
| Home Algorithm — Part 1 (Phase-Based Grids + Price Strip + DOM Reorder) | `index.html` #s-browse: إعادة ترتيب الأقسام (Top Stores انتقل لأعلى بعد Sport) + 12 قسم جديد `display:none` مؤقتاً: `#br-sec-price-low` (شريط "تحت 3,000 دج")، 5 أقسام لون (`#br-sec-color-black/white/brown/wild1/wild2` — wild1/wild2 بعنوان فارغ يُملأ بالـ JS)، 5 vgrids جديدة (`#br-sec-vgrid-6..10`) و`#br-sec-vgrid-close` بلا عنوان قبل Explore مباشرة؛ `app.js`: حذف `_vgridSizePattern` نهائياً، إعادة كتابة `_renderBrowseSections` بمنطق phase-based (`small`<40: vgrid 1-3 بـ[9,6,4] / `medium`≤120: vgrid 1-5 بـ[12,9,6,6,6] / `large`>120: vgrid 1-7 بـ[18,12,9,9,9,9,9])؛ `usedInGrids` Set يتتبّع القطع المستهلكة عبر vgrid واحد فقط (شافل Fisher-Yates واحد)؛ إضافة `_showSec`/`_hideSec` كـ helpers عامة؛ شريط السعر يفلتر `Number(p.price)<3000`؛ vgrid-8/9/10/close وكل أقسام اللون تبقى مخفية ومفرّغة (dormant لحد Part 2)؛ **تغيير سلوك متعمّد**: pool الـ Explore (§12) لم يعد يستثني قطع `#br-sec-repeats` (كان يستثنيها سابقاً) — التعليمات الجديدة حددت الاستثناء بـ`usedInGrids` + anchor فقط، فبعض القطع قد تظهر في كل من Repeats و Explore الآن | يوليو 2026 |
| Home Algorithm — Part 1 تصحيحات (حذف Price Strip + عدد ديناميكي + View All) | حذف `#br-sec-price-low`/`#br-strip-price-low` نهائياً من `index.html` وكل منطقه من `app.js` (كان مضافاً بالمهمة السابقة، تراجع كامل)؛ helper جديد `_stripCount(total)` داخل `_renderBrowseSections` (`min(8,max(2,floor(total/3)))`) يحل محل كل `slice(0,8)` الثابت بشرائط الأقسام (casual/sport/streetwear/classic/oldmoney + New This Week)؛ `_renderHStrip` أصبح يقبل `opts={minCount,totalCount,viewAllKey}` بدل عتبة `<2` الثابتة — شرائط الفئات `minCount:2`، New This Week `minCount:4` (نفس العتبة ستُطبَّق على شرائط اللون بـ Part 2)؛ زر "عرض الكل" (`.br-view-all`، CSS جديد بـ`style.css`) يظهر تلقائياً بعد أي شريط إذا كان العدد الكلي لمصدره (قبل القص) > 8، بـ`onclick` مؤقت `console.log('view-all:',key)` — Part 2 سيربطه فعلياً؛ شرائط Repeats (`#br-repeat-strip-*`) لم تُمس (خارج نطاق هذه المهمة، تستخدم `_renderHStrip` بإعداداتها الافتراضية) | يوليو 2026 |
| Home Algorithm — Part 1 تصحيح ثانٍ (`_stripCount` phase-aware) | `_stripCount` نُقل ليصبح معرّفاً بعد كتلة phase detection/`_PHASE_CFG` داخل `_renderBrowseSections` (كان قبلها، فيمنعه من قراءة `phase`) ليصبح closure عليها؛ الصيغة صارت مرتبطة بالـ phase: `small` → `min(4,total)`، `medium` → `min(8,max(3,floor(total/2.5)))`، `large` → `min(8,max(3,floor(total/3)))` (بدل صيغة واحدة ثابتة `min(8,max(2,floor(total/3)))` لكل الحالات)؛ `newStrip` (New This Week) أيضاً انتقل حسابه لبعد التعريف الجديد لأنه يعتمد على `_stripCount`؛ شرائط اللون (Part 2) خارج النطاق كالمعتاد | يوليو 2026 |
| Home Algorithm — تعديل أحجام vgrid (مساحة تنفّس) | تعديل بسيط بـ`_PHASE_CFG` داخل `_renderBrowseSections`: `small` من `[9,6,4]` إلى `[8,5,4]`، `medium` من `[12,9,6,6,6]` إلى `[11,8,7,5,5]`، `large` من `[18,12,9,9,9,9,9]` إلى `[17,11,8,8,7,7,7]`؛ لا تغيير بعدد الـ grids النشطة ولا بمنطق الـ phase/anchor/`_stripCount` | يوليو 2026 |
| Discover Screen — Zone 2 (بحث) + Zone 3 (شبكة حيّة) | `#s-discover` أصبح 3 مناطق: Zone 1 (فلاتر — لم تُمس، بما فيها زر "أظهر النتائج" وتدفق `#s-results`/`runDiscover` الكامل الذي بقي سليماً كما هو)؛ Zone 2 حقل بحث جديد `#dc-search-input` (RTL، نمط `.cf-input` الموجود)؛ Zone 3 شبكة جديدة `#dc-grid` (`.br-prod-grid`، نفس كلاس Browse)؛ الشبكة تعيد استخدام `_brProds` (المصفوفة المشتركة التي يملأها `loadBrowse()` أصلاً — لا استعلام Supabase جديد؛ إن كانت فارغة عند فتح Discover يُستدعى `loadBrowse()` مرة واحدة فقط ثم `_dcRunLiveFilter()`)؛ `_dcRunLiveFilter()` جديدة تطبّق كل فلاتر Zone 1 (النوع/الميزانية/الفئة/المقاس/اللون — بنفس منطق `runDiscover` لكن بفلترة JS محلية بدل استعلام) + نص البحث (`.toLowerCase().includes()`، مع `_dcStripHtml()` يزيل أي وسم HTML قبل المقارنة أمانًا)؛ تُستدعى تلقائياً من كل callback فلتر موجود (`dcSelectType`/`dcOnSliderInput`/`dcToggleSizeOpt`/`dcToggleCatOpt`/`dcToggleColorSwatch`/`dcClearColors`) عبر إضافة سطر استدعاء واحد بنهاية كل دالة؛ `_dcRenderGrid()` جديدة تُكرر نفس قالب بطاقة `.br-prod-card` الموجود بـ`_renderVGrid` (لم تُعدَّل `_renderVGrid` نفسها لأنها بقائمة "لا تُمس" ولأن سلوكها بإخفاء القسم كاملاً عند الفراغ لا يناسب حاجة Zone 3 لعرض نص فارغ داخل الشبكة)؛ حالة الفراغ: "ما لقينا قطعة بهذا الوصف" (`.dc-grid-empty`) | يوليو 2026 |
| Discover Screen — تصحيحان (موضع زر النتائج + مزامنة الشبكة) | زر "أظهر النتائج" (`.dc-footer`) نُقل من خارج `.dc-body` (كان sibling منفصل بـ`flex-shrink:0` يبقى مثبتاً تحت المحتوى القابل للتمرير) ليصبح آخر عنصر **داخل** `.dc-body` نفسها، بعد `#dc-grid` مباشرة — يتمرّر الآن طبيعياً مع البقية؛ لا تغيير بمنطقه (`dcShowResults`) أو حالة التفعيل؛ `dcShowResults()` صار يستدعي `_dcRunLiveFilter()` أيضاً قبل الانتقال لـ`#s-results` (كانت تُحدَّث فقط عند تغيير فلتر/بحث، الآن تُحدَّث أيضاً عند الضغط على الزر — يبقى `#dc-grid` متزامناً عند العودة لـ Discover)؛ Zone 2/الشبكة أنفسهما لم يُعاد إنشاؤهما (كانا موجودين من مهمة سابقة، `_brProds` أُعيد استخدامها كالمعتاد بلا `_allProds` جديد) | يوليو 2026 |
| Discover Screen — إعادة ترتيب أخيرة (الزر قبل حقل البحث) | ترتيب DOM بـ`#s-discover` صار: نموذج الفلاتر → زر "أظهر النتائج" (`.dc-footer`) → حقل البحث `#dc-search-input` → الشبكة `#dc-grid` (كان الزر بعد الشبكة من المهمة السابقة، انتقل الآن ليسبق حقل البحث مباشرة كما طُلب)؛ لا توجد أي قاعدة `position:fixed`/`sticky`/`bottom:0` بـ`style.css` أو inline على `.dc-footer`/`.dc-results-btn` أصلاً (تأكيد فقط، لا حذف مطلوب)؛ لا تغيير بأي منطق JS — كل ربط `_dcRunLiveFilter`/`_dcRenderGrid`/`_brProds` كان منجزاً من المهام السابقة | يوليو 2026 |
| Discover Screen — تبديل الكل/فلترة + Bottom Sheet (نوع/فئة) | تحت حقل البحث أُضيف toggle جديد بشكلين "الكل"/"فلترة" (`.dc-mode-toggle`/`.dc-mode-pill`) وصف چيبس فلتر مخفي افتراضياً (`#dc-chip-row`، يظهر بـ`animation:panelSlide .2s` عند اختيار "فلترة") فيه چيبان: نوع القطعة (`#dc-chip-type`، أيقونة SVG علّاقة) والفئة (`#dc-chip-cat`، أيقونة SVG تاغ) — كلاهما يفتح bottom sheet جديد `#dc-opt-sheet-overlay` (نفس نمط `.bc-picker` الموجود بالـ Editor: overlay ثابت `align-items:flex-end` + sheet بزوايا علوية مدوّرة + قائمة صفوف قابلة للنقر، مع "الكل" كخيار أول لمسح الفلتر) بدل إعادة استخدام `.bc-picker` نفسه لأنه مرتبط بمنطق Editor الخاص (`_editorProds`/`_bcItems`)؛ خيارات "نوع القطعة" = مفاتيح `_AP_TYPE_SIZES` بتسميات عربية جديدة (`_DC_TYPE_LABELS_AR`)، خيارات "الفئة" = `_CAT_LIST` بعناوين `_CAT_TITLES` الموجودة؛ **`_dcRunLiveFilter()` أُعيدت كتابتها بالكامل**: صارت مبنية فقط على toggle الجديد (`_dcMode`) + چيبين مستقلّين (`_dcChipType`/`_dcChipCat`) + نص البحث — لم تعد تعتمد على حقول نموذج Zone 1 (`_dcType`/الميزانية/`_dcCategory`/المقاس/اللون) التي بقيت محجوزة حصرياً لتدفّق "أظهر النتائج"→`#s-results`/`runDiscover`؛ استدعاءات `_dcRunLiveFilter()` الموجودة أصلاً بنهاية `dcSelectType`/`dcOnSliderInput`/`dcToggleSizeOpt`/`dcToggleCatOpt`/`dcToggleColorSwatch`/`dcClearColors` (من مهام سابقة) لم تُحذف احتراماً لقائمة "لا تُمس" — أصبحت الآن استدعاءات زائدة غير ضارة (تُعيد نفس نتيجة الفلتر الجديد فقط)؛ **تصحيح حقول DB**: المهمة افترضت `p.type` لكلا الچيبين، لكن الكود الفعلي يخزّن نوع القطعة (قميص/بنطلون...) بعمود `product_type` والفئة (casual/sport...) بعمود `type` — طُبِّق الحقل الصحيح لكل چيب بدل اتّباع النص حرفياً | يوليو 2026 |
| Editor — حقل صورة الغلاف (cover_image) | عمود جديد `products.cover_image` (TEXT، nullable) عبر migration مباشرة على Supabase؛ حقل رفع صورة واحدة اختياري جديد بين "صور القطع" و"صور التفاصيل" بـAdd Product modal (`#ap-cover-strip`/`#ap-cover-input`، دوال `_renderCoverStrip`/`addProductCoverImage`/`removeProductCoverImage` — نفس نمط ضغط `_compressImage` والرفع لـ`product-images` الموجود، لكن صورة واحدة بلا حد أقصى شرطي)؛ **منطق thumbnail**: `p.cover_image||p.image` طُبِّق فقط في المواضع الأربعة المذكورة صراحة بالمهمة — Browse grid (`_renderVGrid`/`_exploreRenderBatch`/`renderBrGrid` الميت)، الشرائط الأفقية (`_renderHStrip`)، Discover grid (`_dcRenderGrid`)، وقائمة Saved (`renderSaved` + إضافة `cover_image` لـ`select()` الخاص بـ`saved_items`)؛ بطاقات Editor/Show Mode/Outfits/Build Collection/Results screen لم تُمس (خارج نطاق المهمة صراحة)؛ **Product Detail popup**: `openProdDetail` يُقحم `cover_image` كأول slide في `_pdImages` إذا وُجد (`_pdCoverOffset` جديد يتتبّع 0/1)؛ `pdSelectColor` يُطبّق `idx+_pdCoverOffset` عند القفز للصورة المطابقة للون — `cfSelectColor`/`openOrderForm` (نموذج الطلب) لم يُمسّا لأنهما خارج نطاق "popup" المحدَّد بالمهمة، رغم أنهما يشاركان نفس `_pdImages` (احتمال إزاحة بسيطة بفهرسة اللون بنموذج الطلب لقطع فيها cover_image — لم تُطلب معالجته) | يوليو 2026 |
| Editor — إصلاح cover_image (bug ثنائي) | **Fix 1 (thumbnail)**: فحص شامل لنمط `images[0]`/`images?.[0]` كـthumbnail عبر `app.js` — لا وجود لأي حالة إضافية غير مُعالجة (كل بطاقات العميل تعتمد أصلاً على الحقل الأحادي `p.image` المُطابق لـ`images[0]` وقت الحفظ، وسبق تطبيق `p.cover_image||p.image` عليها بمهمة سابقة)؛ الحالة الوحيدة المتبقية بنمط `images[0]` هي `cfConfirmOrder` (سطر ~2050، بناء `imageUrl` لحمولة الطلب) — استُثنيت لأنها ليست thumbnail بطاقة بل جزء من منطق نموذج الطلب/popup الملحق، ومحمية بقاعدة "لا تُمس منطق تبديل اللون"؛ `admin.html` استُثني بالكامل (شاشة إدارة)؛ **Fix 2 (color chip state)**: `_renderColorCircles` أصبحت تقبل معامل سادس اختياري `noDefaultActive` — عند تفعيله، أول شريحة لون لا تُعلَّم `--active` تلقائياً (بدل `i===0` الافتراضي)؛ `openProdDetail` يمرره كـ`!!p.cover_image` عند نداء `pd-colors-wrap`؛ نداء `cf-prod-color` (نموذج الطلب) لم يُعدَّل بهذه المهمة تحديداً (⚠️ تبيّن لاحقاً أنه كان لا يزال يفتّرض `i===0` نشطة افتراضياً بسبب نقص المعامل السادس — أُصلح بمهمة لاحقة، انظر السجل التالي)؛ هذا يُصلح تعارضاً بصرياً سابقاً كانت فيه أول شريحة لون تظهر "نشطة" رغم أن الكاروسيل يفتح على صورة الغلاف (لا صورة لون) | يوليو 2026 |
| Editor — نموذج الطلب: شرائح اللون تبدأ محايدة (cover_image) | نداء `_renderColorCircles('cf-prod-color',...)` بـ`openOrderForm` (~2003) لم يكن يمرر المعامل السادس `noDefaultActive` رغم إضافته بمهمة سابقة — فكانت أول شريحة لون بنموذج الطلب تظهر `pd-color-circle--active` دائماً حتى مع وجود `cover_image` (بينما الـpopup نفسه أُصلح مسبقاً)؛ أُضيف `!!p.cover_image` كوسيط سادس لنفس النداء، فتبدأ الشرائح محايدة عند وجود غلاف ولا تُفعَّل إلا بلمسة المستخدم (`cfSelectColor`) — لا تغيير عند غياب `cover_image` | يوليو 2026 |
| Editor — إصلاح cover_image (thumbnails المتبقية + نموذج الطلب) | **Fix 1**: `p.cover_image||p.image` طُبِّق على 4 بطاقات إضافية كانت خارج نطاق المهام السابقة — `_edProdCardHtml` (بطاقة Editor Mode، تُستخدم أيضاً لشرائح Main Hero Slider/Hero Slider في Editor عبر `_renderSliderGrid` فلا حاجة لتعديل منفصل هناك)، `_showProdCardHtml` (بطاقة Show Mode)، و`buildBrowseHero` (خلفية Hero Slider بشاشة Browse — عُدِّل أيضاً شرط الفلترة `p.image`→`(p.cover_image||p.image)` وليس فقط سطر `bg:` لأن قطعة بغلاف فقط بلا `images[]` كانت ستُستبعد من الـhero بالكامل قبل الوصول لسطر الخلفية)؛ **Fix 2 (نموذج الطلب/imageUrl)**: طلبت المهمة صيغة "طرح" (`activeColorIdx - _pdCoverOffset`) لـ`openOrderForm`/`cfSelectColor`، لكن بعد تتبّع دقيق لمعنى `data-idx` (فهرس لون خام 0-based، نفس ما يستخدمه `pdSelectColor` بالـpopup الذي **يجمع** الإزاحة لا يطرحها) تبيّن أن الطرح يعرض صورة خاطئة (مثال ملموس: قطعة بغلاف + لونين، اختيار اللون الثاني بالـpopup ثم فتح نموذج الطلب كان سيُرجع صورة الغلاف بدل صورة اللون المختار) — طُرح السؤال على المستخدم فاختار **الجمع** (`idx+_pdCoverOffset`) لتطابق منطق الـpopup تماماً، بينما `cfConfirmOrder` (يفهرس `p.images` مباشرة — عمود لا يحتوي `cover_image` أصلاً) بقي **بلا أي إزاحة** مع fallback جديد `p.cover_image||images[0]` عند فهرس لون غير صالح؛ لم يُطبَّق نص المهمة الحرفي بسبب هذا التناقض المؤكَّد | يوليو 2026 |
| Stock — Part 1 (DB + Add Product grid) | جدول جديد `stock` (product_id/color/size/quantity، PK مركب على الثلاثة، CHECK quantity≥0، RLS: `seller_manages_own_stock` عبر product_id IN sellers' products) عبر Supabase MCP مباشرة؛ **حذف كامل** لتوغل "حالة التوفر" (متوفر/غير متوفر) من Add Product modal — الحقل نفسه بـ`index.html` + دالة `setAvailability` + متغير `_apAvailable` + إزالته من payload بـ`saveProduct` + تنظيف كل الاستدعاءات بـ`_resetModalForm`/`openEditProduct` + حذف CSS `.avail-toggle/.avail-btn`؛ عمود `products.is_available` بقي بالـ DB بلا تغيير (شاشات الزبون/Saved/Product Detail التي تقرأه لم تُمس، خارج النطاق)؛ قسم "المخزون" جديد مكانه بالضبط (بعد وصف قصير، قبل زر Done) — شبكة `#ap-stock-grid` تُبنى ديناميكياً من `_apColorTags`×`_apSizes` (تُعاد بناؤها تلقائياً عند أي تغيير حجم/نوع قطعة عبر `_renderSizeBtns` أو لون عبر `color-tag-btns` listener) وتختفي كلياً (`display:none`) إذا لا ألوان أو لا أحجام مختارة؛ `_apStockQty` (خريطة `color__size`→quantity) تحافظ على القيم المُدخلة عبر إعادة البناء (delegation listener على `#ap-stock-grid`)؛ `saveProduct` يستدعي `_saveStockRows()` جديدة بعد نجاح insert/update (يتطلب الآن `.select('id').single()` على الـinsert لجلب id الجديد) — ترفع upsert فقط للخانات >0 (`onConflict:'product_id,color,size'`)؛ `openEditProduct` أصبحت async — تجيب صفوف `stock` الموجودة لـ product_id وتملأ `_apStockQty` قبل رسم الشبكة | يوليو 2026 |
| Stock — Part 2 (تاب المخزون + 📦 Bottom Sheet) | تاب ثالث جديد "المخزون" بين Catalog وOutfits (`edSwitchTab`/`ed-tab-btn-stock`/`#ed-stock-screen`، نفس نمط الإخفاء/الإظهار الموجود لـ3 أجسام بدل 2)؛ أيقونة صندوق SVG بنفس مقاس/نمط أيقونات التابات الموجودة (14px، `stroke="currentColor"` يرث لون التاب النشط/غير النشط تلقائياً) بدل "18px ذهبي ثابت" المطلوب حرفياً — انحراف متعمّد للحفاظ على تناسق بصري مع Catalog/Outfits (لولاه ستبقى الأيقونة ذهبية حتى وهي غير نشطة)؛ `loadStockScreen()` تعيد استخدام `_editorProds` (الكائن الذي تملأه `loadEditorProducts` أصلاً) بدل استعلام جديد إذا كان معبّأً، وتجيب مجموع `stock.quantity` لكل المنتجات دفعة واحدة (`select('product_id,quantity').in('product_id',ids)`) ثم تجمعها بـJS (لا `GROUP BY` بـSupabase JS مباشرة)؛ صف كل منتج: صورة 48×48 + اسم + شارة "إجمالي: X قطعة" أو "نفد" (خلفية `#7A3B3B`)؛ الضغط على الصف يفتح 📦 sheet؛ **📦 Bottom Sheet** (`#st-sheet-overlay`) يعيد استخدام كلاسات `.dc-opt-sheet-overlay/.dc-opt-sheet` الموجودة أصلاً من شاشة Discover حرفياً (بدل تكرار CSS) + أزرار `.ap-cancel/.ap-done` الموجودة من Add Product modal؛ الشبكة تُبنى من `p.color_tags`×`p.sizes` (المهمة افترضت خطأً حقل `product.colors` — لا وجود له بالسكيمة، الحقل الفعلي `color_tags` كما أُنشئ بـPart 1)؛ زر "حفظ" يرفع upsert لكل خانات الشبكة **بلا استثناء الأصفار** (خلافاً لـPart 1/Add Product) لأن نص المهمة هنا لم يطلب تخطي الأصفار، وتخطيها كان سيمنع تصفير كمية موجودة فعلاً (مثال: بيع آخر قطعة)؛ زر 📦 جديد على بطاقة Editor (`_edProdCardHtml`) بموضع `position:absolute;bottom:5px;left:5px` (مقابل `.ed-prod-dots` أعلى-يمين) يفتح نفس الـsheet مباشرة من الكتالوج | يوليو 2026 |
| Stock — Part 3 (out_of_stock_since + توفر الزبون + إخفاء Browse/Discover) | عمود جديد `products.out_of_stock_since` (TIMESTAMPTZ nullable) عبر migration؛ **F**: `_updateOutOfStockSince(sb,productId,totalQty,currentValue)` جديدة — تُستدعى من `_saveStockRows` (Add Product، Part 1) و`saveStockSheet` (📦 sheet، Part 2) بعد كل حفظ مخزون: totalQty=0 وcurrentValue فارغ → تسجّل الآن (لا تُصفّر الساعة إذا محدَّدة مسبقاً — القيمة الجديدة تُحفظ فوراً بذاكرة `_editorProds` المحلية لتفادي "تصفير الساعة" عند حفظ متكرر بنفس الجلسة قبل أي reload)؛ totalQty>0 → مسح الحقل دائماً؛ **الشرط**: المنطق يُطبَّق فقط إذا كانت شبكة المخزون تحتوي فعلاً inputs (ألوان+أحجام مختارة) — منتج بلا شبكة مخزون لا يُمس حقله إطلاقاً (تفادي تعليم منتجات لم تُستخدم لها الميزة أصلاً كـ"نفدت")؛ **G**: شبكة Add Product (`_renderStockGrid`, Part 1) — القيمة الافتراضية للخانة غير المُدخلة صارت 1 بدل 0 (`_apStockQty[key]!=null?...:1` — تمييز صريح بين "0 محفوظ فعلياً" و"لم تُلمس بعد" حتى لا يُكسر عرض القيم الحقيقية في وضع التعديل)؛ **H (Product Detail popup)**: `_pdLoadStock`/`_pdRenderSizePills`/`_pdAllZero`/`_pdUpdateAvailabilityUI` جديدة + `_pdStockMap`/`_pdHasStock`/`_pdCurrentColorKey` — بعد فتح الـpopup تُجلب صفوف `stock` للمنتج (fire-and-forget، لا تؤخر فتح الـsheet)؛ اختيار لون (`pdSelectColor`) يعيد رسم size pills حسب مخزون ذلك اللون: كمية=0 → pill رمادية `disabled` مع خط عبر النص، أول pill غير صفرية تُصبح active افتراضياً؛ عنصر جديد `#pd-size-oos-note` ("غير متوفر" أحمر صغير) يظهر إن لم يبق أي مقاس متاح للون المختار؛ بانر جديد `#pd-oos-banner` ("هذه القطعة غير متوفرة حالياً") يظهر فقط إذا كل تركيبات لون×مقاس = صفر عبر كل الألوان (ليس فقط اللون المختار)؛ **توافق خلفي حرفي**: لا صفوف `stock` إطلاقاً لمنتج (`_pdHasStock=false`) → كل الأحجام تُعرض متاحة كالسابق تماماً، بلا أي تغيير بصري؛ **I (إخفاء المنتجات النافدة من Browse/Discover)**: الشرط الحرفي بالمهمة يتطلب `EXISTS(SELECT 1 FROM saves WHERE...)` — الجدول الفعلي اسمه `saved_items` وليس `saves` (تصحيح)؛ الأهم: RLS الحالي على `saved_items` (`customers_select_own_saved`) يمنع أي عميل من قراءة حفظيات عميل آخر، فتعذّر تنفيذ شرط EXISTS بالكامل عبر SDK من جهة العميل مباشرة — أُضيفت دالة SQL جديدة **`browse_visible_product_ids()`** (SECURITY DEFINER، بنفس نمط `is_admin()`/`seller_is_approved()` الموجود بالمشروع أصلاً) تُرجع فقط IDs المنتجات المسموح عرضها (بلا كشف أي بيانات عن مَن حفظ ماذا) — لم تُعدَّل أي RLS policy موجودة؛ `loadBrowse()` يستدعي هذه الـRPC ثم يضيف `.in('id',...)` على استعلام `products` الأساسي (يغطي Browse + شبكة Discover الحية Zone 3 تلقائياً لأنها تعيد استخدام نفس `_brProds`)؛ نفس RPC + فلتر أُضيف أيضاً لـ`runDiscover()` (استعلام شاشة Results الرئيسي) ولاستعلام `compProds` ("قطع تتناسق معها") بداخلها — كلاهما جزء من Discover ومسموح تعديل استعلامهما صراحة؛ Editor Mode / Show Mode (`openStoreView`) / deep link (`_openDeepLinkProduct`) / Admin لم تُمس إطلاقاً | يوليو 2026 |
| إصلاحان — تسمية تاب المخزون (لغة) + Cover Image في Hero Slider المتجر | **Fix 1**: نص تاب "المخزون" كان hardcoded عربياً دائماً (بلا مراعاة `_sellerLang`) — لُفَّ بـ`<span id="ed-tab-lbl-stock">` (الافتراضي الآن "📦 Stock" ليطابق نمط Catalog/Outfits الإنجليزي الافتراضي) وأُضيف `setTxt('#ed-tab-lbl-stock',ar?'📦 المخزون':'📦 Stock')` بـ`_applySellerLang`؛ **انحراف**: المهمة افترضت وجود نمط `setTxt` سابق لـCatalog/Outfits بنفس الدالة لتقليده — لا وجود له فعلياً (تباعا لم تُترجَم أصلاً، تبقى إنجليزية ثابتة)، فأُضيف فقط سطر المخزون المطلوب دون اختراع ترجمة لتابين لم تُطلب؛ لم يُستخدم `setTxt` مباشرة على الزر نفسه (كان سيمسح أيقونة الـSVG لأن `textContent=` يحذف كل الأبناء) — أُضيف `<span>` مخصص للنص فقط بدلاً من ذلك؛ **Fix 2**: `buildHeroSlider` (Show Mode + Guest Store View — دالة واحدة مشتركة لكليهما عبر `renderShowProducts`) كانت الموقع الوحيد المتبقي يستخدم `p.image` خام بدل `cover_image` (فلترة `heroProds` + `bg:` بالشرائح)؛ أُضيف helper مشترك `_heroThumb(p)` (معلَّق `// cover_image-aware`) يطبَّق بثلاث نقاط بنفس الدالة (شرط الفلترة الأساسي، fallback الفلترة، وبناء `bg:`)؛ `buildBrowseHero` (Browse الزبون) و`_showProdCardHtml`/`_edProdCardHtml` (بطاقات الشبكة العادية) كانت مُصلحة أصلاً من مهام سابقة — لم تُمس | يوليو 2026 |
| Stock UX — إزالة مقاسات نافدة + ترتيب الألوان حسب التوفر | **Fix 1**: `_pdRenderSizePills` لم تعد تعرض مقاسات الكمية=0 كـ`disabled`/مشطوبة — تُستبعد كلياً من الـDOM (`sizes.filter` بدل `map` مع علامة oos)؛ كلاس `.pd-size-pill--oos` حُذف نهائياً (JS+CSS، لا استخدام متبقٍ)؛ بانر "هذه القطعة غير متوفرة حالياً" (`#pd-oos-banner`, شرط `_pdAllZero`) لم يحتج تعديلاً — كان مطابقاً للمطلوب أصلاً من مهمة سابقة؛ **Fix 2**: ترتيب شرائح اللون حسب التوفر (`_pdRenderColorChips` جديدة) يعمل فقط بعد استقرار `_pdLoadStock` (يُستدعى من داخلها بعد جلب صفوف `stock`، بدل نداء مباشر لـ`_renderColorCircles`)؛ **مشكلة تقنية أساسية حُلَّت**: الترتيب المرئي الجديد لا يمكن أن يُغيّر `data-idx` المستخدم بواسطة `pdSelectColor` للقفز لصورة الكاروسيل الصحيحة (لأن مصفوفة `_pdImages` تبقى بترتيب `_pdColorKeys(p)` الأصلي غير المُرتَّب) — أُضيف معامل سابع اختياري `originalKeys` لـ`_renderColorCircles` يُستخدم لحساب `data-idx=originalKeys.indexOf(key)` بدل فهرس الحلقة عند وجوده؛ نداء `cf-prod-color` (نموذج الطلب) لم يُعدَّل ولم يُمرَّر له المعامل الجديد — يحافظ تماماً على سلوكه القديم (فهرس الحلقة كالمعتاد)؛ الترتيب مستقر (`Array.sort` قياسي في JS الحديث) فتُحافَظ على الترتيب النسبي الأصلي بين الألوان المتساوية بالتوفر | يوليو 2026 |
| Stock UX — تراجع عن الترتيب/الإزالة، رجوع لمنطق بسيط | **Revert 1**: `_pdRenderSizePills` رجعت تعرض كل المقاسات دائماً (لا `.filter()`) — مقاس بكمية=0 يُعرض كزر عادي طبيعي + `<span class="pd-size-pill-oos-lbl">غير متوفر</span>` بجانبه مباشرة بنفس الصف (أحمر مطفأ `#E53935`، Tajawal، 10px) — بلا `disabled` وبلا أي تعطيل/تعتيم؛ **أثر جانبي متوقّع وغير مُعالَج عمداً**: بما أن أول مقاس (`i===0`) يُعلَّم active دائماً بغض النظر عن كميته الآن، `#pd-size-oos-note` (الملاحظة العامة تحت الصف كله، من مهمة سابقة) تصبح شبه ميتة عملياً (`hasActive` شبه دائماً true) — لم تُمس `_pdUpdateAvailabilityUI`/`#pd-oos-banner`/`_pdAllZero` إطلاقاً وفق طلب المهمة صراحة؛ **Revert 2**: حُذفت `_pdRenderColorChips` نهائياً؛ نداءا الرسم في `openProdDetail` وداخل نهاية `_pdLoadStock` رجعا لـ`_renderColorCircles` المباشرة بلا فرز — الأول بـ`activeKey=null` (كالأصل)، الثاني بـ`activeKey=_pdCurrentColorKey` (حُفظ عمداً لأنه "غير ضار" ويمنع فقدان اختيار المستخدم بعد استقرار جلب المخزون)؛ المعامل السابع `originalKeys` حُذف كلياً من توقيع `_renderColorCircles`، و`data-idx` رجع لفهرس الحلقة البسيط `i`؛ نداء `cf-prod-color` (نموذج الطلب، ~س2243) لم يُمس إطلاقاً — كان أصلاً بـ6 معاملات فقط | يوليو 2026 |
| Stock UX — مؤشر توفر واحد ديناميكي (استبدال is_available القديم) | حُذف اعتماد `#pd-stock-dot`/`#pd-stock-lbl` على `p.is_available` القديم بالكامل من `openProdDetail` — يُضبطان الآن على قيمة افتراضية "متوفر"/أخضر عند فتح الـpopup فقط، بانتظار تحميل المخزون؛ دالة جديدة `_pdSyncAvailability()` (منسوخة حرفياً كما طُلب) تفحص `_pdStockMap[_pdCurrentColorKey]?.[_pdCurrentSizeKey]` — لا لون/لا حجم مُختار أو لا صفوف `stock` أصلاً → افتراضي "متوفر" دائماً؛ تُستدعى من 3 مواضع فقط: نهاية `_pdLoadStock` (بعد بناء `_pdStockMap`)، نهاية `pdSelectColor`، ونهاية `pdSelectSize` (لم تكن تستدعي أي شيء سابقاً)؛ متغير جديد `_pdCurrentSizeKey` (بمستوى الموديول مع البقية) يُصفَّر عند فتح كل منتج ويُملأ فقط عبر لمسة المستخدم الفعلية لمقاس (`pdSelectSize` يقرأ `btn.dataset.size` — أُضيف `data-size` لأزرار المقاس بكل من الرسم الأولي المتزامن والرسم اللاحق بـ`_pdRenderSizePills` لأنه لم يكن موجوداً أصلاً، وكان النص الظاهر فقط)؛ **إزالة المؤشرات الثانوية**: الوسم الفردي `<span class="pd-size-pill-oos-lbl">` حُذف كلياً من `_pdRenderSizePills` (رجعت الدالة لأبسط شكل: كل المقاسات تُعرض دائماً، أول واحد active افتراضياً، بلا أي منطق كمية)؛ `#pd-size-oos-note` و`#pd-oos-banner` لم يُحذفا من الـHTML لكنهما يُخفيان دائماً (`display:none`) عند فتح أي منتج، و`_pdUpdateAvailabilityUI` (التي تتحكم بهما) لم تعد تُستدعى من أي مكان — بقيت معرَّفة فقط كما طُلب حرفياً؛ CSS: أُضيف `.pd-stock-dot--ok`/`.pd-stock-dot--oos`، وحُذفت الكلاسات الميتة `.pd-stock-dot--no`/`.pd-stock-lbl--yes`/`.pd-stock-lbl--no`/`.pd-size-pill-oos-lbl` (لا استخدام متبقٍ لأي منها بعد هذا التغيير)؛ **ملاحظة سلوك معروفة وغير مُعالَجة عمداً (تنفيذ حرفي للمواصفة)**: تغيير اللون عبر `pdSelectColor` يستدعي `_pdRenderSizePills` التي تُعيد تعليم أول مقاس active بصرياً، لكنها لا تُصفِّر `_pdCurrentSizeKey` — فقد يبقى المؤشر يتحقق من مقاس اختاره المستخدم قبل تغيير اللون بينما الواجهة تُظهر مقاساً مختلفاً كـ"نشط" بصرياً، لحد لمسة مقاس جديدة تُصحّح الحالة | يوليو 2026 |
| Stock — إصلاح جذري: لا سياسة RLS للقراءة العامة (كل ميزة توفر المخزون كانت معطّلة فعلياً للزبائن) | **التشخيص**: سياسة RLS الوحيدة على جدول `stock` كانت `seller_manages_own_stock` (`FOR ALL`، مقيّدة بـ`product_id IN (SELECT id FROM products WHERE seller_id=auth.uid())`) — لا سياسة SELECT عامة إطلاقاً؛ بما أن الزبون (مسجّل أو ضيف) ليس بائع المنتج أبداً، كل استعلام `stock` من `_pdLoadStock` كان يُرجع مصفوفة فارغة `[]` بصمت (ليس خطأ — RLS يُصفّي الصفوف فقط) لكل منتج ولكل زبون دائماً؛ فاقم الأمر أن `_pdLoadStock` (`const{data:rows}=await sb.from('stock')...`) لا تلتقط `error` إطلاقاً ولا تُسجّله — فحتى لو كان هناك خطأ حقيقي مستقبلاً سيمر بصمت تام؛ **النتيجة**: كل منطق `_pdHasStock`/`_pdStockMap`/المؤشر الديناميكي/شارات النفاد المبني عبر عدة مهام سابقة كان معطَّلاً فعلياً من جهة الزبون الحقيقي طوال الوقت (كان يعمل فقط لو اختبره البائع نفسه وهو مسجّل دخول بحسابه)؛ **الإصلاح**: سياسة جديدة `public_read_stock` (`FOR SELECT USING (true)`) أُضيفت — القراءة مفتوحة للجميع الآن (Postgres يجمع سياسات RLS بـOR لكل أمر)، بينما الكتابة (INSERT/UPDATE/DELETE) تبقى حصراً للبائع المالك عبر `seller_manages_own_stock` الموجودة أصلاً (لم تُعدَّل ولم تُحذف) | يوليو 2026 |
| Stock — إصلاح `_pdCurrentSizeKey` الهرم عند تغيير اللون | `pdSelectColor` أصبحت تُصفّر `_pdCurrentSizeKey` إلى `_pdProd.sizes?.[0]??null` بعد `_pdRenderSizePills` وقبل `_pdSyncAvailability` — يطابق الآن أول مقاس يُعلَّم active بصرياً بعد تغيير اللون، بدل بقاء المفتاح على آخر مقاس لمسه المستخدم قبل التبديل؛ نفس الـpopup المشترك بين الزبون والبائع (`openProdDetail` واحدة) فالإصلاح يسري على الاثنين معاً | يوليو 2026 |
| Deep Link — معامل `?color=` | `cfConfirmOrder` يُلحق `&color=`+`selColorIdx` (الفهرس الخام المستخدم أصلاً لـ`imageUrl`، بلا إزاحة غلاف) بـ`productLink` عند وجود لون محدَّد فقط (`selColorIdx>-1`) — `pdOrderWhatsApp` لم يُمسّ (نص فقط، لا رابط بارامترات)؛ `_handleProductDeepLink`/`_openDeepLinkProduct` يقرآن ويمرّران `colorIdx` (تحقّق `/^\d+$/` قبل `parseInt`، `undefined` إذا غائب/غير رقمي)؛ `openProdDetail(id,initialColorIdx)` معامل ثانٍ اختياري جديد — كل نداءات `openProdDetail(id)` الحالية تبقى صالحة بلا تغيير؛ **انحراف عن نص المهمة**: المهمة طلبت استدعاء `pdSelectColor(initialColorIdx+_pdCoverOffset)` مباشرة، لكن `pdSelectColor` يتوقع عنصر DOM (`btn.classList`) لا رقماً — استدعاؤها برقم كان سيسبب خطأ تشغيل فوري؛ بدلاً من ذلك يُبحث عن شريحة `#pd-colors-wrap .pd-color-circle[data-idx="${initialColorIdx}"]` المطابقة وتُستدعى `pdSelectColor(chip)` عليها — نفس نتيجة لمسة المستخدم الحقيقية بلا إزاحة مضاعفة ودون تعديل `pdSelectColor` نفسها | يوليو 2026 |
| Launch Control — DB + Admin Panel | **DB**: جدول جديد `system_settings` (key TEXT PK/value TEXT) مع صف واحد `('launch_mode','demo')`؛ RLS: SELECT عام، UPDATE عبر `is_admin()` فقط (نفس نمط `admin_update_sellers` الموجود)؛ عمود جديد `sellers.is_demo` (BOOLEAN NOT NULL DEFAULT false)، يُحدَّث عبر سياسات `admin_update_sellers`/`seller owns row` الموجودتين أصلاً بلا سياسة جديدة؛ **Admin UI**: شريط "Launch Control" جديد (`#adm-launch-bar`) أعلى `#adm-panel` (فوق `.adm-header` والتابات) بزرين "تجريبي 🧪"/"حقيقي 🚀" — `loadLaunchMode()`/`setLaunchMode(mode)` جديدتان، تُستدعى `loadLaunchMode()` من `revealPanel()` عند فتح اللوحة؛ تبديل per-seller `is_demo` أُضيف كـ pill صغير (`_sellerDemoPillHtml`/`toggleSellerDemo`) داخل بطاقة كل بائع بتاب "البائعون" (يعتمد على استعلام إضافي منفصل `sellers.select('id,is_demo')` بعد الـ RPC `get_pending_sellers_with_email` — RPC نفسه لم يُعدَّل احتراماً لقاعدة "لا تُمس منطق الموافقة")؛ `app.js` لم يُمس إطلاقاً — فلترة الواجهة الأمامية حسب `launch_mode`/`is_demo` مؤجلة لمهمة لاحقة | أغسطس 2026 |
| Launch Control — وضع ثالث "الكل" | زر ثالث `#launch-btn-all` ("الكل 🌐") أُضيف بين تجريبي/حقيقي بـ`#adm-launch-bar`؛ `setLaunchMode`/`loadLaunchMode`/`_reflectLaunchBtns` تدعم الآن القيم الثلاث (`demo`/`all`/`production`) — toast "يظهر كل المحتوى" عند اختيار الكل؛ القيمة الافتراضية المزروعة بـ`system_settings.launch_mode` غُيّرت من `'demo'` إلى `'all'` مباشرة بالـDB | أغسطس 2026 |
| Admin — قائمة البائعين المقبولين (مؤقتة) | قسم مضغوط جديد "البائعون المقبولون" تحت قائمة البائعين قيد المراجعة بتاب "البائعون" — `loadApprovedSellers()` جديدة (`sellers WHERE approval_status='approved'`, تُستدعى من `revealPanel()`)، صف بسيط اسم+pill تبديل `is_demo` يعيد استخدام `_sellerDemoPillHtml`/`toggleSellerDemo` الموجودتين حرفياً بلا تعديل؛ **تغيير هيكلي مطلوب**: مخرجات `loadSellers()` (البائعون قيد المراجعة) نُقلت لحاوية داخلية جديدة `#ptab-sellers-pending` بدل الكتابة مباشرة على `#ptab-sellers` نفسها — بدون هذا الفصل كان `innerHTML=` الخاص بـ`loadSellers()` سيمحو القسم الجديد أدناه بكل تحديث؛ لا ترقيم صفحات ولا بحث — القسم "مؤقت وبسيط عمداً" كما طُلب صراحة | أغسطس 2026 |
| Launch Mode — فلترة Browse + Discover + Top Stores | متغيّر جديد بمستوى الموديول `_launchMode='all'` بـ`app.js` (قراءة فقط بالواجهة الأمامية — لا كتابة إطلاقاً، `admin.html` هو الكاتب الوحيد لـ`system_settings`)؛ `_loadLaunchMode()` جديدة تُستدعى بأول سطر داخل معالج `DOMContentLoaded` الرئيسي (قبل أي `navigateTo`/`loadBrowse` محتمل) فتُضمن قيمة `_launchMode` جاهزة قبل أي عرض؛ **`loadBrowse()`**: `is_demo` أُضيف لحقول `seller:sellers(...)` بالاستعلام، وفلتر JS إضافي بعد فلتر `hero_status` الموجود (`demo`→يُبقي بائعين `is_demo===true` فقط، `production`→يستبعدهم، `all`→بلا فلترة) قبل تعيين `_brProds` النهائية — يغطي شبكة Discover الحية (Zone 3) تلقائياً لأنها تعيد استخدام نفس `_brProds`؛ **`runDiscover()`** (استعلام Results الرئيسي فقط، `~س2995`): نفس الإضافتين (`is_demo` بالـselect + فلتر `results` بعد فلتر `hero_status`) — استعلام "قطع تتناسق معها" المُكمِّل لم يُمس (خارج ما طلبته المهمة صراحة، فقط `runDiscover`/`dcInitSlider` مذكورتان)؛ **`openStoreView()`**: أصبحت `async` (كل نداءاتها الثلاثة fire-and-forget فبقيت صالحة بلا تعديل) — بوضع `demo` تتحقق أولاً من `sellers.is_demo` للمتجر المطلوب عبر استعلام مباشر، وتُوقف الفتح مع toast "متوفر يوم الإطلاق ✦" إن لم يكن `is_demo===true` (أو عند فشل التحقق نفسه — فشل مغلق وليس مفتوح)؛ `loadShowMode`/`loadEditorProducts`/`browse_visible_product_ids`/نموذج الطلب/Outfits/Collections لم تُمس إطلاقاً | أغسطس 2026 |
| Launch Mode — Top Stores كـ teasers بوضع تجريبي | `loadBrowse()`: بعد فلتر `_launchMode` (الذي يُبقي `_brProds` على بائعين تجريبيين فقط بوضع `demo`)، إن كان الوضع `demo` يُنفَّذ استعلام إضافي منفصل `sellers.select('id,store_name,profile_image,is_demo').eq('approval_status','approved')` يجيب **كل** البائعين المعتمدين (تجريبي وحقيقي معاً) ثم `renderTopStores()` يُستدعى مرة ثانية بهذه القائمة الكاملة فوق ما رسمته `_renderBrowseSections` أصلاً (التي ترى فقط بائعين تجريبيين لأن `_brProds` مُصفّاة مسبقاً) — تصحيح تلقائي شبه فوري بلا حاجة لتعديل `_renderBrowseSections` نفسها؛ **`renderTopStores()`**: خريطة جديدة `byId` (id→store) من قائمة `list` المُمرَّرة؛ عند بناء onclick لكل أيقونة متجر، إن كان `_launchMode==='demo'` والبائع `is_demo!==true` → `onclick` يصبح `toast('متوفر يوم الإطلاق ✦')` فقط بدل `openStoreView` (الأيقونة نفسها تُرسم عادية، بلا أي تعتيم بصري — لم يُطلب ذلك)؛ خارج وضع `demo` السلوك يبقى كالسابق تماماً بلا أي تغيير | أغسطس 2026 |
| Launch Mode — بانر تنبيه للبائعين الحقيقيين | عنصر `.demo-mode-banner` جديد (`style.css`) — خلفية ذهبية شفافة 12%، حد سفلي ذهبي، نص "التطبيق في وضع الاختبار — ستظهر نتائجك عند الإطلاق"؛ أُضيف مرتين بـ`index.html` مباشرة تحت الـtopbar بكل من `#s-editor` (`#editor-demo-banner`) و`#s-show` (`#show-demo-banner`)، كلاهما `display:none` افتراضياً؛ **`app.js`**: متغيّر موديول جديد `currentSeller` (يُملأ فقط داخل `loadEditorProducts()` من صف `sellers` الخاص بالمستخدم المسجَّل دخوله كبائع — يبقى `null` دائماً لجلسات الزبون/الضيف)؛ `is_demo` أُضيف لاستعلام `sellers` بـ`loadEditorProducts()`؛ `_syncDemoBanner()` جديدة (`_launchMode==='demo' && currentSeller?.is_demo!==true` → إظهار)، تُستدعى من 3 مواضع: نهاية `loadEditorProducts()` (بعد استقرار `currentSeller`)، نهاية `loadShowMode()` (بعد استدعاء `loadEditorProducts()` غير المنتظَر — تصحيح ذاتي شبه فوري بنفس نمط Top Stores)، وبعد `await _loadLaunchMode()` مباشرة بمعالج `DOMContentLoaded` (لحالة استعادة الجلسة)؛ **أمان ضمني**: `loadShowMode()` يُوقف تنفيذه مبكراً (`if(_guestSellerId)return`) قبل الوصول لأي من الاستدعاءين الجديدين عند معاينة زبون لمتجر بوضع ضيف — فلا يظهر البانر إطلاقاً على أي شاشة يراها الزبون، ولا حاجة لتعديل مسار الضيف (`loadGuestStoreProducts`) نفسه | أغسطس 2026 |
| Analytics — Event Tracking (Prompt 1: توسيع event_type + نداءات جديدة) | **DB**: CHECK constraint `user_behavior_log_event_type_check` وُسِّع من (`view`/`save`/`store_visit`) ليشمل `whatsapp_order`/`form_order`/`whatsapp_store`/`ref_wa`؛ **اكتشاف RLS حرج غير مطلوب بالمهمة لكن ضروري لعملها**: سياسة `ubl_public_insert_events` (تسمح بإدراج anon/authenticated بشرط `user_id IS NULL`) كانت مقيَّدة بنفس 3 قيم `event_type` القديمة فقط — بدون توسيعها كانت كل أحداث `whatsapp_order`/`form_order`/`whatsapp_store` من زبائن غير مسجَّلين (الحالة الأغلب لأن الطلب عبر واتساب/النموذج لا يتطلب تسجيل دخول) ستُرفض بصمت من RLS رغم نجاح الإدراج ظاهرياً بالكود (نفس نمط "silent failure" المتكرر بالمشروع)؛ وُسِّعت لتشمل القيم الأربعة الجديدة أيضاً؛ **`app.js` — نداءات جديدة**: `pdOrderWhatsApp` يُلحق `&ref=wa` بالرابط + `_logEvent('whatsapp_order',...)`؛ `storeWhatsApp` (زر واتساب المتجر المستقل عن المنتج، لا `cfConfirmOrder` كما افترضت المهمة حرفياً — `cfOrderWhatsApp` بداخلها فقط تُفوِّض لـ`pdOrderWhatsApp` فلا حاجة لنداء مزدوج) → `_logEvent('whatsapp_store',{sellerId:_storeShare.id})`؛ `cfConfirmOrder` → `_logEvent('form_order',...)` فقط داخل فرع `res.ok` (نجاح Google Sheets)، باستخدام `p`/`p.id`/`p.seller_id` الفعليين (لا `_cfProd` — متغيّر غير موجود بالكود، افتراض خاطئ بنص المهمة)؛ `_handleProductDeepLink` أُضيف له بلوك إضافي فقط (لا لمس للتدفق الأصلي) يفحص `?ref=wa` ويُسجّل `ref_wa` حصراً إذا كان الفاتح بائعاً مسجَّل دخوله؛ **`_logEvent` نفسها أُعيدت كتابتها** (async IIFE بدل `.then/.catch`): تجيب `auth.getSession()` أولاً وتُرسل `user_id` الآن عند وجوده (لم يكن يُرسَل إطلاقاً سابقاً)؛ **Revisit**: لـ`event_type='view'` مع مستخدم مسجَّل، تفحص وجود صف `view` سابق لنفس `product_id`+`user_id` بالجدول، فتكتب `action='revisit'` بدل `'view'` إذا وُجد؛ **Save-then-order**: لـ`whatsapp_order`/`form_order` مع مستخدم مسجَّل، تفحص `saved_items` (لا `saves` — نفس تصحيح سابق) لنفس `product_id`+`customer_id`، فتكتب `action='save_then_order'` إذا وُجد؛ كلا الفحصين يُتخطَّيان صامتاً للزوار غير المسجَّلين (لا `user_id` للمقارنة) | أغسطس 2026 |
| Analytics — تاب الإحصاء بلوحة الإدارة (Prompt 2) | تاب رابع جديد `#stab-analytics`/`#ptab-analytics` ("📊 الإحصاء") أُضيف بعد "كل المنتجات" في `admin.html`؛ `switchTab()` الموجودة (لم تُعدَّل بنيتها) حصلت على سطر إضافي واحد (`if(tab==='analytics'&&!_anLoaded)loadAnalytics()`) لتحميل كسول عند أول فتح فقط، زر "🔄 تحديث" يعيد التشغيل يدوياً دائماً؛ **6 بطاقات سريعة** (`loadAnalyticsCards`, تُقرأ مرة واحدة): تجار مقبولون (`sellers.approval_status='approved'`) + منتجات نشطة/مخفية (`products.is_hidden`) + إجمالي مشاهدات/محفوظات/طلبات (`user_behavior_log.event_type`) — عبر `count:'exact',head:true` (نمط جديد بالمشروع، لم يُستخدم بأي مكان آخر بـ`admin.html`)؛ **فلتر زمني** (اليوم/الأسبوع/الشهر pills، افتراضي "اليوم") يُعيد حساب مشاهدات/طلبات الفترة + "أكثر ساعة نشاطاً" (تجميع `EXTRACT(HOUR)` يدوياً بـJS عبر `getHours()` محلي، لأن Supabase JS SDK لا يدعم `GROUP BY` — نفس نمط التجميع اليدوي المستخدم سابقاً بـStock Part 2)؛ **3 قوائم Top 5** (أكثر مشاهدة/طلباً/زيارة) تُبنى بنفس منطق التجميع اليدوي ثم join لأسماء المنتجات/المتاجر عبر استعلام `.in('id',ids)` إضافي؛ **مؤشرات تحويل** (طلبات بعد حفظ/عودة لنفس القطعة/تحقق واتساب) تقرأ `action='save_then_order'`/`action='revisit'`/`event_type='ref_wa'` مباشرة؛ **"مشاهدات بدون طلبات"**: مبني من نفس صفوف قائمة "أكثر مشاهدة" مطروحاً منها أي `product_id` ظهر بصفوف الطلبات (لا استعلام NOT EXISTS منفصل، إعادة استخدام البيانات المجلوبة أصلاً لتفادي استعلام رابع)؛ **تصحيح حرج على نص المهمة**: عمود "المتجر" بجدول `user_behavior_log` المستخدم فعلياً لأحداث `store_visit` هو `seller_id` (كتبته `_logEvent`)، وليس `store_id` كما افترضت المهمة — العمود `store_id` موجود أيضاً بالجدول لكنه يُكتب فقط من `_logBehavior` (مسار `save`/`unsave` القديم، بلا علاقة بـ`store_visit`)؛ استعلام القائمة الثالثة استخدم `seller_id` الصحيح؛ لا لمس لأي تاب/منطق موجود ولا لـ`app.js` | أغسطس 2026 |
| Analytics — فصل أنواع الطلبات بفلتر الفترة (Prompt 3) | داخل قسم "النشاط حسب الفترة" بتاب الإحصاء: البطاقة المدمجة القديمة "الطلبات في الفترة" (`whatsapp_order`+`form_order` معاً) حُذفت من `loadAnalyticsPeriod()`؛ `#an-period-cards` أصبحت تعرض بطاقة واحدة فقط (المشاهدات في الفترة، `style="grid-column:1/-1"` لتمتد كامل العرض بدل ترك نصف الصف فارغاً بشبكة الـ2-عمود الموجودة)؛ حاوية جديدة `#an-order-types` (بين `#an-top-hour` وقسم "أكثر المنتجات مشاهدة") تعرض 4 صفوف بسيطة بنمط `.adm-an-list-row`/`.adm-an-list-name`/`.adm-an-list-count` الموجود أصلاً (لا CSS جديد): نوايا واتساب (`event_type='whatsapp_order'`) + طلبات مؤكدة/استمارة (`event_type='form_order'`) + تحقق البائع (`event_type='ref_wa'`) + عودة لقطعة (`action='revisit'`) — الأربعة مفلترة بنفس `created_at>=startIso` للفترة المختارة وتُعاد بـ`Promise.all` واحدة مع بقية استعلامات `loadAnalyticsPeriod` فتتحدث تلقائياً عند تغيير الـpill (`anSetPeriod` لم تُعدَّل، تستدعي نفس الدالة)؛ لا لمس للبطاقات الست العلوية ولا لقوائم Top 5 ولا لمؤشرات التحويل ولا لأي تاب آخر ولا لـ`app.js` | أغسطس 2026 |
| Outfits Phase 2 — Customer Detail Sheet يطابق الـ Blueprint (checkboxes + مقاسات + سعر ديناميكي) | `openOutfitDetailPublic` أُعيدت كتابتها بالكامل: القطع الأفقية القابلة للتمرير (`#od-pieces`/`.od-piece`) استُبدلت بقائمة عمودية جديدة (`#od-pieces-list`/`.od-piece-row`) — كل صف: صورة 72×90 (`chosen_image\|\|product.image`) + اسم + سعر ذهبي + صف مقاسات (`product.sizes`، pill واحد نشط) + نقطة خضراء "متوفر" (`product.is_available`) + checkbox 22×22 (مُفعَّل افتراضياً)؛ النقر على الصف نفسه لم يعد يفتح `openProdDetail` (كان `onclick` قديم على الصف بالكامل) — استُبدل بتفاعل الـcheckbox/المقاس فقط، لا تنقل؛ **حالة جديدة بمستوى الموديول** `_odSelection` (مصفوفة `{itemId,product,chosenImg,selected,chosenSize}`) تُبنى من `outfit.items` عند الفتح، وتُحدَّث عبر `odTogglePiece(idx)`/`odSelectSize(idx,size)` الجديدتين؛ `_odUpdateTotal()` جديدة تجمع أسعار القطع المُحدَّدة فقط وتكتبها في `#od-total-price` (استبدل `#od-price` الثابت القديم المبني على `_colPriceLabel`/`total_price`/`is_exclusive`) + تُعطّل زري السلة/واتساب (`disabled`) إذا لا قطعة مُحدَّدة؛ **الفوتر** (`.od-actions` القديم) استُبدل بـ`.od-footer` بثلاثة أزرار: `#od-heart-btn` (نُقل من فوق الغلاف — أيقونة ♡ فقط الآن، لا زر heart منفصل على الصورة) + `#od-cart-btn` جديد (`odOpenCart()` → `toast('قريباً — طلب متعدد القطع')`) + `#od-wa-btn` (نص فقط الآن، بلا أيقونة SVG قديمة)؛ **`odOrderWhatsApp()` أُعيدت كتابتها بالكامل**: تُصفّي القطع المُحدَّدة فقط، تتطلب مقاس لكل قطعة (`toast('اختر مقاس لكل قطعة')` إن نقص)، رسالة جديدة بصيغة "قائمة قطع + مقاس + سعر لكل قطعة + المجموع" (لا رابط `?outfit=` كالسابق — الصيغة الجديدة نصية بحتة حسب مواصفة المهمة)؛ استعلام `renderCollectionsPublic` (outfit_items→product) وُسِّع بـ`sizes,is_available` (كانا ناقصين، مطلوبان للصفوف الجديدة)؛ fallback نص الوصف الفارغ تغيّر من "لا يوجد وصف لهذه التنسيقة." إلى "لا يوجد وصف" (مطابقة حرفية لنص المهمة) | أغسطس 2026 |
| Outfits Phase 2 — Footer Layout + Seller Settings Gating | `.od-footer` أُعيد هيكلته ليطابق الـ blueprint: صف 1 بعرض كامل زر "Save" (حد ذهبي، خلفية شفافة، نص ذهبي، Fraunces) — نفس `odToggleHeart()` القديم لكن النص أصبح "Save"/"Saved ✓" بدل ♡/♥؛ صف 2 جديد `.od-footer-row-btm` (flex row) يحوي `#od-cart-btn` (أيقونة حقيبة + "أضف إلى السلة"، حد ذهبي شفاف) و`#od-wa-btn` (أيقونة واتساب + "اطلب عبر واتساب" + "تواصل مباشر مع المتجر"، خلفية ذهبية) — كلاهما `flex:1` فيتساويان، وإذا اختفى أحدهما يأخذ الآخر العرض الكامل تلقائياً بلا CSS إضافي؛ **Gating جديد يطابق `openProdDetail` حرفياً**: `_storeShare` (المتغير المشترك لبيانات المتجر المعروض بـ#s-show) اكتسب حقلين جديدين `whatsapp_enabled`/`cart_enabled` (افتراضياً `false` حتى تُحمَّل بيانات البائع الحقيقية) — تُملأ من نفس استعلام `sellers` الموجود أصلاً بـ`loadGuestStoreProducts` (أُضيف `cart_enabled` لقائمة الأعمدة المطلوبة، لا استعلام إضافي)؛ `openOutfitDetailPublic` يحسب `canWa`/`canCart` بنفس صيغة `openProdDetail` (`p.seller.phone && whatsapp_enabled!==false` / `!!cart_enabled`) ويُخفي/يُظهر الزرين + كامل الصف 2 إذا الاثنان معطّلان (Save فقط يبقى ظاهراً)؛ **إصلاحان جانبيان اكتُشفا أثناء الربط**: (1) `#od-wa-btn` سابقاً كان يظهر فقط بشرط وجود `phone`، بلا أي تحقق من `whatsapp_enabled` — ثغرة gating حقيقية؛ (2) `odOrderWhatsApp()` لم يكن فيه أي toast عند بائع معطِّل لواتساب (خلافاً لـ`cfOrderWhatsApp` بنموذج الطلب) — أُضيف نفس الحارس/الرسالة حرفياً؛ **معروف وغير مُعالَج عمداً (خارج نطاق هذه المهمة)**: `#od-heart-btn`/بطاقات `.of-card` بالشبكة العامة لا تتحقق فعلياً من `saved_outfits` عند الفتح — الحالة تُصفَّر دائماً لغير محفوظ حتى لو كانت التنسيقة محفوظة فعلاً؛ يحتاج استعلام إضافي لكل تنسيقة (auth + join)، قرار بنية أكبر يستحق عرضه على بوجمعة أولاً وليس "سد ثغرة" بسيطة | أغسطس 2026 |
| Deep Link Outfit — إصلاح سباق `_guestSellerId` (وضع Demo) | **التشخيص**: `_openDeepLinkOutfit(storeId,outfitId)` كان يستدعي `openStoreView(...)` **بلا `await`** ثم مباشرة `switchShowTab('collections',...)`+`renderCollectionsPublic()`؛ `openStoreView` بوضع `launch_mode==='demo'` يبدأ بـ`await` فعلي (فحص `sellers.is_demo`) **قبل** تعيين `_guestSellerId` — فكان الاستدعاء غير المنتظَر يترك التنفيذ يتابع فوراً بينما `_guestSellerId` لا يزال بقيمته القديمة (غالباً `null` لأول زيارة)، فتُنفَّذ `renderCollectionsPublic()` بـ`sellerId=null`→ `_colOutfits=[]` بصمت، فلا تُفتح ورقة تفاصيل التنسيقة رغم فتح صفحة المتجر بنجاح؛ بوضع `all`/`production` (لا `await` داخل `openStoreView` قبل التعيين) كان السلوك سليماً صدفةً — الخلل يظهر فقط بوضع `demo`؛ **الإصلاح**: أُضيف `await` أمام `openStoreView(...)` + حارس `if(_guestSellerId!==storeId)return;` بعدها مباشرة (يغطي أيضاً حالة رفض المتجر بالكامل بوضع demo — بائع غير تجريبي — حيث `openStoreView` يعود مبكراً بـtoast بلا تنقل، فيمنع الحارس محاولة تبديل تاب على شاشة لم تُفتح أصلاً)؛ **تحقق E2E**: اختبار Playwright بمتصفح حقيقي مع Supabase REST مُموَّه بالكامل (`page.route`) وتأخير اصطناعي 400ms على فحص `is_demo` لإعادة إنتاج نافذة السباق تحديداً — نجح الاختبار: ورقة التنسيقة فُتحت تلقائياً بالاسم الصحيح وتاب Collections نشط؛ لا لمس لـ`_handleStoreDeepLink`/`openOutfitDetailPublic`/`renderCollectionsPublic` أنفسها | أغسطس 2026 |

---

## المرحلة الحالية
**Phase 3 — UI/UX**
- Hero Slider تلقائي (أقوى القطع / عروض البائع)
- Horizontal Sections (عروض / تخفيضات / رياضي / جديد)
- Product Cards (صورة + نجوم + وفرة + سعر)
- حواف ناعمة + خلفية فخمة + انتقالات سلسة
- Full-Screen Immersive Mode للتنسيقات
- Variable Reward في الـ Browse (محتوى غير متوقع)

**Restructure (موازي) — Editor Mode + DB foundation**
- ✅ Phase 1: DB migration (`slider_type`) + فصل Editor لـ 3 أقسام — منتهي
- 🔜 Phase 2: ربط جهة الزبون (buildHeroSlider/buildBrowseHero تقرأ `slider_type` بدل `hero` القديم) + إصلاح bug الضغط على الـ hero
- 🔜 Phase 3: تلميع بصري (ارتفاع الـ hero 480px، إلخ)

**Home Algorithm (موازي) — Part 1/2**
- ✅ Part 1: DOM reorder + phase-based vgrid sizing (small/medium/large) + عدد ديناميكي بـ`_stripCount` + عتبات ظهور (2 للفئات/4 للـNew) + زر View All — منتهي (لا يوجد price strip، أُلغي)
- 🔜 Part 2: أقسام اللون (black/white/brown/wild1/wild2) + تفعيل vgrid-6..10 وvgrid-close + color-aware rendering + ربط View All فعلياً

---

## قواعد الكود الثابتة
- **Diff Style فقط** — لا تعيد كتابة ملف كامل، عدّل ما يحتاج تعديل
- **لا تكسر ما يشتغل** — كل تعديل يكمل ما هو موجود
- **الحل الأبسط دائماً** — لا تعقيد غير ضروري
- **AI via Edge Functions** — لا مفاتيح API في الـ frontend؛ كل نداء لـ Claude يمر عبر Supabase Edge Function
- **supabase-config.js** — لا تلمسه، لا ترفعه
- **XSS — esc() + safeUrl()** — كل string من DB يُحقن في `innerHTML` يمر عبر `esc()`؛ كل URL يُستخدم في `src` أو `style background-image` يمر عبر `safeUrl()`؛ استخدم `textContent` حيثما أمكن
- **products.slider_type** (`none`/`hero`/`main_hero`) هو المرجع الوحيد لمكان ظهور القطعة — عمود `hero` (bool) القديم محذوف نهائياً. ⚠️ `buildHeroSlider`/`buildBrowseHero` بـ app.js لسا يقرأو `p.hero` القديم (دائماً undefined الآن) — لازم تحديثهم لـ `p.slider_type==='hero'`/`'main_hero'` في Restructure Phase 2 وإلا الـ hero slider عند الزبون يبقى فاضي

---

## الهوية البصرية — لا تتغير
```css
--bg:    #0B0A08   /* أسود دافئ */
--gold:  #D2AF69   /* ذهبي رئيسي */
--rust:  #A83D22   /* أحمر CTA */
--card:  #1C1915   /* بطاقات */
--txt:   #D8CFBA   /* نص عام */
```
- خطوط: Fraunces (عناوين) + Tajawal (عربي)
- أنيميشن: بطيء ناعم — cubic-bezier springs
- لا ألوان زاهية — فخامة هادئة دائماً

---

## خارج النطاق الآن 🚫
لا تقترح هذه الميزات في Phase 3:
- Camera Scan للملابس
- نظام اشتراكات مدفوع
- React Native / Flutter
- PWA conversion
- Style Passport
- Outfit Rating + QR

---

## قاعدة التحديث التلقائي
**بعد كل مهمة مكتملة — قبل الانتقال للتالية:**
1. حدّث قسم "ما تم إنجازه" بالمهمة الجديدة
2. حدّث "المرحلة الحالية" إذا تغيرت
3. أضف أي قاعدة جديدة اكتشفتها لقسم "قواعد الكود"

---

*آخر تحديث: أغسطس 2026 | Claude Code — Deep Link Outfit: إصلاح سباق `_guestSellerId` بوضع Demo*
